<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eglisianhoxha.albanianpanel">

    <!-- Required permissions for floating overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlbanianPanel">

        <!-- FloatingPanelService Declaration -->
        <service
            android:name=".FloatingPanelService"
            android:enabled="true"
            android:exported="false"
            android:description="@string/floating_panel_service_description" />

        <!-- Main Activity or other activities go here -->

    </application>

</manifest>
